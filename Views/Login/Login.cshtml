@model MVCCore.Models.Login
@{
    ViewBag.Title = "User Login";
    // Layout = "~/Views/Shared/_Layout.cshtml";
}

@using (Html.BeginForm())
{

    <div class="limiter">
        <div class="container-login100">
            <div class="wrap-login100 p-l-55 p-r-55 p-t-65 p-b-50">


                <span class="login100-form-title p-b-33">
                    Account Login
                </span>

                <div class="editor-label">
                    @Html.LabelFor(m => m.UserName)
                </div>
                <div class="editor-field custominputfield">
                    @Html.TextBoxFor(m => m.UserName, new { placeholder = "Enter Username" })
                    @Html.ValidationMessageFor(m => m.UserName)
                </div>

                <div class="editor-label">
                    @Html.LabelFor(m => m.Password)
                </div>
                <div class="editor-field custominputfield"  >
                    @Html.PasswordFor(m => m.Password, new { placeholder = "Enter Password"})                     
                    @Html.ValidationMessageFor(m => m.Password)
                </div>
                
                <div class="divbutton">                    
                    <input type="submit" value="Log In" class="custombutton"  />     
                    <input type="submit" value="Register" formaction=@Url.Action("Register") formmethod="post" class="custombutton" />
                     
                </div>
                <div>
                    
                    @ViewBag.SuccessMessage
                </div>
            </div>
        </div>

    </div>
}
 